const express = require("express");
//TODO: complete API help routes
const router = express.Router();
const { } = require("../controllers/apiController");
const { protect } = require("../middleware/authMiddleware");

// Prefix: /api/help/
// router.get("/", );

module.exports = router;
