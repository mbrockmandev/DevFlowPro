const asyncHandler = require("express-async-handler");

const User = require("../models/userModel");
const Ticket = require("../models/ticketModel");

//TODO: Add functionality to demonstrate API test routes
// going to mimic routes being used in a limited fashion
// will write to separate, test database which is reset on server start
